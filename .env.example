# ====================================
# 网络设备 SSH 登录凭据配置文件
# Network Device SSH Credentials Configuration
# ====================================
#
# 📋 这个文件是用来做什么的？
#    存储 SSH 登录网络设备（交换机、路由器）的用户名和密码
#    这样程序就可以自动登录设备采集信息，无需每次手动输入
#
# 🔐 为什么需要单独的 .env 文件？
#    1. 安全：密码不会被打包分发或提交到代码仓库
#    2. 方便：一次配置，多次使用
#    3. 灵活：可以为不同环境使用不同的凭据
#
# 💡 "DEVICE" 是什么设备？
#    DEVICE = 网络设备的统称，包括：
#      - 交换机 (Switch)：Cisco、华为、H3C、Juniper、Arista、HP 等
#      - 路由器 (Router)：所有品牌的路由器
#    这是通用变量名，适用于所有厂商的网络设备
#
# ====================================
# 使用说明 (Ubuntu/Linux):
# ====================================
#   1. 复制此文件: cp .env.example .env
#   2. 编辑文件: nano .env 或 vim .env
#   3. 填入您的实际 SSH 用户名和密码
#   4. 保存并退出 (nano: Ctrl+X, Y, Enter)
#
# 使用说明 (Windows):
#   1. 复制此文件: copy .env.example .env
#   2. 编辑文件: notepad .env
#   3. 填入您的实际 SSH 用户名和密码
#   4. 保存并关闭
#
# ⚠️ 安全提示：
#   - 请勿将 .env 文件分享给他人（包含密码）
#   - 请勿提交到 Git 或任何版本控制系统
#   - 建议使用只读权限的设备账号
# ====================================

# ====================================
# 通用设备凭据（推荐 - 支持所有厂商）
# ====================================
# 说明：
#   - DEVICE_USERNAME: SSH 登录用户名（例如：admin, cisco, huawei）
#   - DEVICE_PASSWORD: SSH 登录密码
#   - DEVICE_SECRET: Enable/特权模式密码（Cisco 需要，其他厂商可选）

DEVICE_USERNAME=admin
DEVICE_PASSWORD=your_password_here
DEVICE_SECRET=your_enable_password_here

# Cisco 专用凭据（可选 - 向后兼容）
# CISCO_USERNAME=admin
# CISCO_PASSWORD=your_password_here
# CISCO_SECRET=your_enable_password_here

# 可选：MySQL/PostgreSQL数据库连接
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=network_inventory
# DB_USER=db_user
# DB_PASSWORD=db_password
